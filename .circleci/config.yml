version: 2.1
executors:
  default: 
    docker: 
      - image: cimg/base:stable
commands: 
  install-aws:
    steps:
      - python3 --version
jobs:
  get-aws-version:
    executor: aws-cli/default
    steps:
      - install-aws
workflows:
  default:
    jobs:
      - get-aws-version

